type Query {
  hello: String
  date: Date
  loggedUser: User
  featuredProduct: Product
  numbersArray: [Int!]!
  users: [User]
  user(id: Int): User
  profiles: [Profile]
  profile(id: Int): Profile
}